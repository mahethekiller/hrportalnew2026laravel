# API Creation Prompt

Generate Api Versioned (V1) REST endpoints for `[ModelName]`.
- Secure routes using Laravel Sanctum middleware.
- Include rate-limiting rules.
- Return structured API Resource classes.
- Write Swagger (L5-Swagger) annotation parameters.