# Test Generator Prompt

Write a comprehensive PHPUnit feature test suite for the `[ModuleName]` module.
- Test authentication gates (unauthorized users must be blocked).
- Test validation boundaries (submit empty or incorrect formats).
- Use transactions rollback between tests.