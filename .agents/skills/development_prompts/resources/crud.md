# CRUD Generation Prompt

Given the database schema for the table `[TableName]` and its Eloquent model `[ModelName]`, generate standard Laravel 12 CRUD components.
- Generate: Form Request validation, Web Controller, Service class, Repository, and Blade forms/tables.
- Strictly adhere to strict types (`declare(strict_types=1);`).